'use client';

export default function DashboardPage() {
  return (
    <div>
      <p className="text-sm text-gray-600">
        Minimal page. Next step is fetching bulls from the backend.
      </p>
    </div>
  );
}
